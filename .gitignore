node_modules/
dist/
.env
*.log
.claude/
# Cached raw live-API window pulls used by the migration importers (server/src/db/lib/liveWindow.js)
.live-cache/
# Database dumps. server/db/schema.sql (structure only) is tracked on purpose; full data
# dumps are not -- they are hundreds of MB and contain live business data.
*-backup.sql
*.dump.sql
gsuite_erp*.sql

# Database dumps -- hundreds of MB of live financial data. Never in git.
backups/

# Temporary 2026-sync artifacts (hold Railway creds / are throwaway) -- see server/RESUME-2026-SYNC.md
server/.env.railway
server/sync-2026.log
server/migrate-2026.log
server/run-2026-sync.sh
